[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for every route so React Router / hash routing works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
